import { FindInListPipe } from './find-in-list-pipe';

describe('FindInListPipe', () => {
  it('create an instance', () => {
    const pipe = new FindInListPipe();
    expect(pipe).toBeTruthy();
  });
});
